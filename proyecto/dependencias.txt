Este Archivo contiene todo lo que se necestia instalar para el ambiente (se asume que ya django esta instalado y el virtualenv)

#pip
pip install psycopg2

#instalar en su computadora (UBUNTU)
POSTGRES:
  sudo apt-get install postgresql postgresql-contrib
